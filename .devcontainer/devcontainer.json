{
  "build": { "dockerfile": "Dockerfile" },
  "customizations": {
    "vscode": {
      "extensions": ["dbaeumer.vscode-eslint", "svelte.svelte-vscode"]
    }
  },

  "forwardPorts": [5173],
  "postCreateCommand": "npm install && pip3 install -r requirements.txt && pre-commit install --install-hooks",
  "postStartCommand": "nohup bash -c 'npm run dev &'"
}
